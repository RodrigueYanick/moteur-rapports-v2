spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/moteurRapport
    username: postgres
    password: 20040
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true